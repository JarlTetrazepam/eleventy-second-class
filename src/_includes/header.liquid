<header>
    <nav>
        <ul>
            {%- for post in collections.posts -%}
                <li><a href="{{ post.url }}">{{ post.data.pageTitle }}</a></li>
            {%- endfor -%}
        </ul>
    </nav>
</header>